I detect **implementation** intent - explicit "Write the documentation." All context is provided in the prompt file. Writing directly.
Done. Documentation written to `docs/components/localstack.md` — matches the exemplar structure, uses manifests as source of truth, and preserves accurate content from the existing page (emulated services list, troubleshooting patterns, auto-initialization flow).
