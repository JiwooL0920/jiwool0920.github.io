I detect **implementation** intent — write a documentation page for RedisInsight. The prompt template is missing manifests, exemplar, and existing content. I need to gather those from the codebase first.
Fired two explore agents to find manifests and doc exemplars. Waiting for results.
