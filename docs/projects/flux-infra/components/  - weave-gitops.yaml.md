I detect **implementation** intent — write a documentation page for weave-gitops. The prompt template has empty sections for manifests, exemplar, and existing content. I need to gather those from the codebase first.
Fired two explore agents to gather manifests and exemplar docs. Waiting for results before writing.
