I detect **implementation** intent - writing a documentation page for Redis Sentinel. But the prompt template is missing all source material (manifests, exemplar, existing content). I need to gather these from the codebase first.
Agents launched to find manifests and exemplar docs. Waiting for results.
